<Window x:Class="OkurleigaHF.PropertyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OkurleigaHF"
        mc:Ignorable="d"
        Title="Property" Height="427.999" Width="808" Loaded="Window_Loaded">
    <Grid >
        <Button x:Name="btnNewProp" Content="Nýskrá leiguhúsnæði" HorizontalAlignment="Left" Margin="33,53,0,0" VerticalAlignment="Top" Width="244" Height="37" Click="btnNewProp_Click"/>
        <DataGrid ItemsSource="{Binding}" AutoGenerateColumns="False" Height="244" Margin="33,121,29.333,0" VerticalAlignment="Top" Grid.RowSpan="2">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Heimilisfang" Binding="{Binding Path=Address}"/>
                <DataGridTextColumn Header="Póstnúmer" Binding="{Binding Path=ZipCode}"/>
                <DataGridTextColumn Header="Stærð" Binding="{Binding Path=PropertySize}"/>
                <DataGridTextColumn Header="Mánaðarleg greiðsla" Binding="{Binding Path=RentCost}"/>
                <DataGridTextColumn Header="Á skrá" Binding="{Binding Path=IsAvailable}"/>
            </DataGrid.Columns>

        </DataGrid>

    </Grid>
</Window>
